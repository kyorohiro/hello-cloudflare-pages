{
	"name": "hello-cloudflare",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler pages deploy --project-name=kyorohiro-hello --branch=main",
		"dev": "wrangler pages dev",
		"start": "wrangler pages dev"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250919.0",
		"wrangler": "^4.38.0"
	},
	"dependencies": {
		"hono": "^4.9.8"
	}
}
